# Server Configuration
PORT=3000
HOST=0.0.0.0

# Database Configuration (PostgreSQL)
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=your_password
PGDATABASE=fun_notifications

# Redis Configuration (Optional, falls back to in-memory mock if empty)
# REDIS_URL=redis://localhost:6379
